// Service layer placeholder for future API/integration connectors
// (Kept clean without mock or backend code per requirements)

export const apiClient = {
  // To be implemented in subsequent phases
}
