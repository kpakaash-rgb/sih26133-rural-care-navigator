# Rural Care Navigator — Backend Dependencies
# FastAPI core
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Pydantic v2 + settings management
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.36

# PostgreSQL driver (sync, battle-tested)
# Switch the driver by changing DATABASE_URL in .env — no code changes needed.
psycopg2-binary==2.9.9

# JWT Authentication
pyjwt==2.9.0

# Environment variable loading
python-dotenv==1.0.1

# HTTP client (used in adapter layer and testing)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
